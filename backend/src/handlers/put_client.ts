export function put_client(): void {
    console.log('put_client');
    // TODO register a client, send them reference photo and subscribe them to updates
    // res.write(JSON.stringify({'msg': `client added to picture ${picture_id}`}));
    // res.end();
}
