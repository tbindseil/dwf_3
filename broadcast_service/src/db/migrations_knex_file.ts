// TODO use prettier import plugin (at least to remove unused imports automatically)
// https://www.npmjs.com/package/prettier-plugin-organize-imports
import { knexConfig } from './knex_file';

export default knexConfig;
